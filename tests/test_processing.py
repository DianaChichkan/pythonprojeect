import pytest

from src.processing import filter_by_state, sort_by_date


@pytest.mark.parametrize("test_list, state, result_list", [

]
assert dictionary_inform["state"] == "EXECUTED"

    assert (filter_by_state) == list_of_dict_sorted


def sort_by_date():
    pass
